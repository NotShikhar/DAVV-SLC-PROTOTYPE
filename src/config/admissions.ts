/** Admission-portal route paths — one source of truth (avoids scattered strings). */
export const ADMISSION_ROUTES = {
  landing: "/admissions",
  login: "/admissions/login",
  apply: "/admissions/apply",
  console: "/admissions/console",
} as const;
